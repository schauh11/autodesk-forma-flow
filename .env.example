# Forma Flow Configuration
# Most settings are managed via the web UI (Settings page).
# You generally don't need to edit this file.

# Environment
NODE_ENV=development

# Optional: Override the data directory (default: %LOCALAPPDATA%\FormaFlow)
# FORMA_FLOW_DATA_DIR=C:\path\to\custom\data
